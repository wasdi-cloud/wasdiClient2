import { ConvertBytesToHigherUnitPipe } from './convert-bytes-to-higher-unit.pipe';

describe('ConvertBytesToHigherUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertBytesToHigherUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
