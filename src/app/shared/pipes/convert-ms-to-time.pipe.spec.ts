import { ConvertMsToTimePipe } from './convert-ms-to-time.pipe';

describe('ConvertMsToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMsToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
